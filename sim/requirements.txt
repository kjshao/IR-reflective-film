# Optional dependencies for sim/optimize_film.py
matplotlib>=3.5
# Global thickness methods: method=de | dual_annealing
scipy>=1.9
# Optional GPU TMM (NVIDIA CUDA only): use_cuda true / rt_engine=tmm_cuda
# Install a wheel that matches your CUDA toolkit, e.g.:
#   pip install cupy-cuda12x
# cupy-cuda12x; platform_system=="Linux"
